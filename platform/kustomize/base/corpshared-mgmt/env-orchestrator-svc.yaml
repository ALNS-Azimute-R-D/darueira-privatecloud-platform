apiVersion: apps/v1
kind: Deployment
metadata:
  name: drr-env-orchestrator-svc
  namespace: drr-corpshared-mgmt
  labels:
    app.kubernetes.io/name: drr-env-orchestrator-svc
    app.kubernetes.io/component: environment-engine
    darueira.io/tier: enterprise-shared
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: drr-env-orchestrator-svc
  template:
    metadata:
      labels:
        app.kubernetes.io/name: drr-env-orchestrator-svc
        darueira.io/tier: enterprise-shared
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: env-orchestrator-svc
          image: localhost:8082/drr-env-orchestrator-svc:v1alpha1
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 8082
              name: http
          env:
            - name: PORT
              value: "8082"
            - name: DRR_AUTHZ_URL
              value: "http://drr-iam-authz-svc.drr-corpshared-plat.svc.cluster.local:8080"
            - name: KAFKA_TOPIC
              value: "drr.environment.events"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 250m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8082
            initialDelaySeconds: 3
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8082
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: drr-env-orchestrator-svc
  namespace: drr-corpshared-mgmt
  labels:
    app.kubernetes.io/name: drr-env-orchestrator-svc
    darueira.io/tier: enterprise-shared
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: drr-env-orchestrator-svc
  ports:
    - port: 8082
      targetPort: 8082
      name: http
