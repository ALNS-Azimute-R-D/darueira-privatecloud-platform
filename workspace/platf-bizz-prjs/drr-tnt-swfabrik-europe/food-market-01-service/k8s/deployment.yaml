apiVersion: apps/v1
kind: Deployment
metadata:
  name: food-market-01-service
  namespace: drr-tnt-swfabrik-europe-marketplaces-dev
  labels:
    app.kubernetes.io/name: food-market-01-service
    app.kubernetes.io/part-of: swfabrik-europe-marketplaces
    darueira.io/tier: tenant-workload
    darueira.io/tenant: swfabrik-europe
    darueira.io/project: marketplaces
    darueira.io/env: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: food-market-01-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: food-market-01-service
        app.kubernetes.io/part-of: swfabrik-europe-marketplaces
        darueira.io/tier: tenant-workload
        darueira.io/tenant: swfabrik-europe
        darueira.io/project: marketplaces
        darueira.io/env: dev
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: app
          image: localhost:8082/swfabrik-europe/food-market-01-service:1.0.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          ports:
            - containerPort: 8081
              name: http
          env:
            - name: SERVER_PORT
              value: "8081"
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://central-postgres.drr-corpshared-plat.svc.cluster.local:5432/drr_tnt_bizapps_db?currentSchema=schm01"
            - name: SPRING_DATASOURCE_USERNAME
              value: "drr_admin"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "change-me-in-openbao"
            - name: SPRING_RABBITMQ_HOST
              value: "message-broker-rabbitmq.drr-corpshared-plat.svc.cluster.local"
            - name: SPRING_RABBITMQ_PORT
              value: "5672"
            - name: SPRING_RABBITMQ_USERNAME
              value: "drr_admin"
            - name: SPRING_RABBITMQ_PASSWORD
              value: "darueira-admin123"
            - name: SPRING_RABBITMQ_VHOST
              value: "/"
            - name: APP_MARKET_ID
              value: "MKT-EU-01-SPRING"
            - name: APP_RABBITMQ_TOPIC
              value: "marketplace.foodtrading.topic"
            - name: APP_RABBITMQ_QUEUE
              value: "marketplace.foodtrading.queue01"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 768Mi
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 20
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: food-market-01-service
  namespace: drr-tnt-swfabrik-europe-marketplaces-dev
  labels:
    app.kubernetes.io/name: food-market-01-service
    darueira.io/tier: tenant-workload
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: food-market-01-service
  ports:
    - port: 8081
      targetPort: 8081
      name: http
